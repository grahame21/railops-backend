from typing import Any, Dict

def transform_trainfinder_payload(raw: Dict[str, Any]) -> Dict[str, Any]:
    # TODO: if you need to rename fields, set direction arrows, etc.,
    # do it here. For now, just return as-is to keep it simple.
    return raw
